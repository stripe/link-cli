# Changelog

## Unreleased

- Preserve empty `capabilities`, `external_connection`, `bank_account` and `card` objects when re-encoding a `Source`.
- Add optional idempotency keys to SpendRequest creation.
- Initial Go SDK implementation.
